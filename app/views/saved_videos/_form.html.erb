<%= form_with(model: saved_video, local: true) do |form| %>
  <% if saved_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saved_video.errors.count, "error") %> prohibited this saved_video from being saved:</h2>

      <ul>
        <% saved_video.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :video_id %>
    <%= form.number_field :video_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
