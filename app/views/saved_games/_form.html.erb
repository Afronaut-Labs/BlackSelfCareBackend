<%= form_with(model: saved_game, local: true) do |form| %>
  <% if saved_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saved_game.errors.count, "error") %> prohibited this saved_game from being saved:</h2>

      <ul>
        <% saved_game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.number_field :game_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
